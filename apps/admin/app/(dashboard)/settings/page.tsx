import { DashboardShell } from '@/features/dashboard/DashboardShell';

export default function SettingsPage() {
  return (
    <DashboardShell title="설정">
      <p>설정 페이지는 다음 단계에서 확장합니다.</p>
    </DashboardShell>
  );
}
